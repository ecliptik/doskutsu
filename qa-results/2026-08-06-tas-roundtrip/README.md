# tas roundtrip

**NOT A BENCHMARK**

A record-versus-replay trace pair from binary 0282, used to find the TAS input-hold bug fixed in nx 0283. `TR` is the recording pass, `TP` the replay. No frame-rate figures should be taken from it.

Round definitions, the canonical round-1 datasets, and how to read a cell log:
[`../README.md`](../README.md).
