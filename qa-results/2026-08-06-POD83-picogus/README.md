# POD83 picogus

**SUPERSEDED -- do not quote**

Binary v1.6.5 `b421e5a52fea`. The first benchmark round, 9 cells, of which 4 truncated the reel's last two transitions and therefore rendered less work than a complete cell. Predates `[fps-true]` (patch 0289), so its frame rates come from the `uclock` timebase that later proved unreliable.

Round definitions, the canonical round-1 datasets, and how to read a cell log:
[`../README.md`](../README.md).
